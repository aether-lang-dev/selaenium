name = "selenium"
version = "0.1.0"
target = "erlang"

description = "Selenium WebDriver for Gleam — over the Aether Selenium core's C NIF on the BEAM (shared with the Erlang and Elixir bindings)."
licences = ["Apache-2.0"]

[dependencies]
gleam_stdlib = ">= 0.34.0 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
