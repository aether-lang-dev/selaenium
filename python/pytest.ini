[pytest]
# The no-browser FFI facts plus the two live-Chrome suites (test_live_chrome,
# test_live_surface). The live tests pytest.skip when chromedriver is absent, so
# the suite is green on a box without a browser.
testpaths = test
python_files = test_*.py
addopts = -ra
