# Pinned Aether toolchain versions for CI — the single place to bump.
#
# aeb tags are "later means later" markers, NOT semver promises; pin them so a
# CI run is reproducible. `ci/toolchain.sh` reads these; override at the shell to
# test a bump (e.g. `AETHER_REF=v0.591.0 ci/run.sh`).

# The Aether compiler (`ae`). Tag vX.Y.Z, branch, or commit SHA.
# 0.590.0 is the floor: it ships std.http.ws_connect (the WebSocket client the
# BiDi work uses — see docs/WebDriver-BiDi.md).
AETHER_REF=v0.590.0

# The aeb build runner. Tag v0.NNN, branch, or commit SHA.
# v0.291 keeps the v0.287 exit-code fix (a .tests.ae node's non-zero return
# reddens the build; without it the presubmit gate is falsely green) and the
# per-platform .tar.gz + .sha256 (so ci/toolchain.sh fetches & VERIFIES a prebuilt
# aeb, no source compile). Since v0.287 it also folded in the 6 SDK
# command-builders this port contributed, added a "shadowing local vs top-level
# setter" footgun fix (20a3c4df4), and itself raised its ae pin to 0.636.0.
AEB_REF=v0.291
